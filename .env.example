# ============================================
# Twitch Drops Bot Web 管理平台配置
# ============================================

# ------------------
# 管理员账户配置
# ------------------
# 管理员用户名（用于登录管理后台）
ADMIN_USERNAME=admin

# 管理员密码（生产环境请务必修改！）
ADMIN_PASSWORD=admin

# ------------------
# JWT 认证配置
# ------------------
# JWT密钥（生产环境必须修改为随机字符串！）
# 生成方法: openssl rand -hex 32
JWT_SECRET_KEY=your-secret-key-change-in-production

# JWT算法（建议保持默认）
JWT_ALGORITHM=HS256

# JWT过期时间（小时）
JWT_EXPIRATION_HOURS=24

# ------------------
# 服务器配置
# ------------------
# 服务器监听地址（0.0.0.0允许外部访问）
SERVER_HOST=0.0.0.0

# 服务器端口
SERVER_PORT=8000

# ------------------
# 数据库配置
# ------------------
# SQLite数据库路径
DATABASE_URL=sqlite:///./app.db

# ------------------
# 文件路径配置
# ------------------
# C# Bot的config.json路径（相对于后端目录）
CONFIG_FILE_PATH=./config.json

# C# Bot的日志目录路径（相对于后端目录）
LOGS_DIRECTORY=./logs

# ------------------
# CORS 跨域配置
# ------------------
# 允许的前端域名（开发模式）
# 生产环境中，后端会直接服务前端静态文件，不需要CORS
CORS_ORIGINS=["http://localhost:8080","http://localhost:3000"]

# ------------------
# Twitch OAuth 配置
# ------------------
# Twitch客户端ID（使用Android App的公开ID用于设备流认证）
# 无需修改，除非Twitch更改了公开客户端ID
TWITCH_CLIENT_ID=kimne78kx3ncx6brgo4mv6wki5h1ko

# ============================================
# 快速开始
# ============================================
# 1. 复制此文件为 .env
#    cp .env.example .env
#
# 2. 修改管理员密码和JWT密钥
#    ADMIN_PASSWORD=your_secure_password
#    JWT_SECRET_KEY=$(openssl rand -hex 32)
#
# 3. 启动服务
#    docker compose up -d
#
# 4. 访问 http://localhost:8000
#
# ============================================
